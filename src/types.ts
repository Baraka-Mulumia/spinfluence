export type NavigationItemType = {
  label: string;
  path: string;
  isSpy?: boolean;
};

export type ServiceType = {
  title: string;
  description: string;
  body: string;
  image: string;
  slug: string;
};
